import { version } from '../../package.json';

const payload = {
    "openapi": "3.0.0",
    "info": {
        "title": "Homium API",
        "description": "Homium API documentation",
        "version": version
    }
};

export default payload;